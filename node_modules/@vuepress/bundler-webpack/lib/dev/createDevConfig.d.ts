import * as Config from 'webpack-chain';
import { App } from '@vuepress/core';
import type { WebpackBundlerOptions } from '../types';
export declare const createDevConfig: (app: App, options: WebpackBundlerOptions) => Promise<Config>;
