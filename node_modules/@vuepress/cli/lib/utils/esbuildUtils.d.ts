import type { BuildOptions } from 'esbuild';
/**
 * Create esbuild `BuildOptions` for `buildToCode`
 */
export declare const createBuildToCodeOptions: (filePath: string) => BuildOptions & {
    write: false;
};
/**
 * Take a file as entry point, and build it to cjs code
 */
export declare const buildToCode: (filePath: string) => Promise<string>;
/**
 * Sync version fo `buildToCode`
 */
export declare const buildToCodeSync: (filePath: string) => string;
