import type { App, ThemeObject } from '../types';
/**
 * Recursively resolve plugins and layouts of theme and its parent theme
 */
export declare const resolveTheme: (app: App, themeName: string) => {
    plugins: ThemeObject[];
    layouts: Record<string, string>;
};
