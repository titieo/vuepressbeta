"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePlugin = void 0;
const shared_1 = require("@vuepress/shared");
const utils_1 = require("@vuepress/utils");
/**
 * Resolve a plugin according to name or path
 */
const resolvePlugin = (pluginName) => {
    const pluginEntry = utils_1.path.isAbsolute(pluginName)
        ? pluginName
        : utils_1.requireResolve(shared_1.normalizePackageName(pluginName, 'vuepress', 'plugin'));
    if (pluginEntry === null) {
        return null;
    }
    const required = require(pluginEntry);
    // allow default export
    return utils_1.hasExportDefault(required) ? required.default : required;
};
exports.resolvePlugin = resolvePlugin;
