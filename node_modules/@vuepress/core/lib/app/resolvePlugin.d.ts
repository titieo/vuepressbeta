import type { Plugin, PluginObject, PluginOptions } from '../types';
/**
 * Resolve a plugin according to name or path
 */
export declare const resolvePlugin: <T extends PluginOptions = PluginOptions, U extends PluginObject = PluginObject>(pluginName: string) => Plugin<T, U> | null;
