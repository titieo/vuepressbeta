/**
 * Directory util function
 */
export declare type AppDirFunction = (...args: string[]) => string;
/**
 * Directory utils
 */
export interface AppDir {
    cache: AppDirFunction;
    temp: AppDirFunction;
    source: AppDirFunction;
    dest: AppDirFunction;
    public: AppDirFunction;
    client: AppDirFunction;
}
/**
 * Environment flags
 */
export interface AppEnv {
    isProd: boolean;
    isTest: boolean;
    isDev: boolean;
    isDebug: boolean;
    nodeEnv: string | undefined;
}
/**
 * Write temp file util
 */
export declare type AppWriteTemp = (file: string, content: string) => Promise<string>;
