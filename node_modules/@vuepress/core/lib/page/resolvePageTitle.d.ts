import type { PageFrontmatter, PageHeader } from '../types';
/**
 * Resolve page title from frontmatter / file content
 */
export declare const resolvePageTitle: ({ content, frontmatter, headers, }: {
    content: string;
    frontmatter: PageFrontmatter;
    headers: PageHeader[];
}) => string;
